precision: 32
strategy: ddp
max_epochs: 150
gpus: -1
num_nodes: 1
sync_batchnorm: ${model.sync_batchnorm}
num_sanity_val_steps: 10
limit_val_batches: 1.0
accumulate_grad_batches: 1
gradient_clip_val: 3.0
replace_sampler_ddp: False
resume_from_checkpoint: