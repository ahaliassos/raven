idim: 512
adim: ${model.visual_backbone.adim}
aheads: ${model.visual_backbone.aheads}
eunits: ${model.visual_backbone.eunits}
elayers: ${model.visual_backbone.elayers}
transformer_frontend: conv1d
transformer_input_layer: ${model.visual_backbone.transformer_input_layer}
dropout_rate: 0.1
transformer_attn_dropout_rate: 0.1
transformer_encoder_attn_layer_type: rel_mha
macaron_style: ${model.visual_backbone.macaron_style}
use_cnn_module: ${model.visual_backbone.use_cnn_module}
cnn_module_kernel: 31
zero_triu: False
a_upsample_ratio: 1
relu_type: swish
ddim: ${model.visual_backbone.ddim}
dheads: ${model.visual_backbone.dheads}
dunits: 2048
dlayers: ${model.visual_backbone.dlayers}
lsm_weight: 0.1
transformer_length_normalized_loss: False
rel_pos_type: latest
layerscale: ${model.visual_backbone.layerscale}
init_values: ${model.visual_backbone.init_values}
ff_bn_pre: ${model.visual_backbone.ff_bn_pre}
post_norm: ${model.visual_backbone.post_norm}
gamma_zero: ${model.visual_backbone.gamma_zero}
gamma_init: ${model.visual_backbone.gamma_init}
mask_init_type: ${model.visual_backbone.mask_init_type}
ctc_type: ${model.visual_backbone.ctc_type}
drop_path: ${model.visual_backbone.drop_path}
mtlalpha: ${model.visual_backbone.mtlalpha}